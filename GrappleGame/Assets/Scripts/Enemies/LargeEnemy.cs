using UnityEngine;

public class LargeEnemy : EnemyAction
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("Large Enemy Created");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
